/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1'], // Permitir el dominio de 127.0.0.1 para las imágenes
    },
};

export default nextConfig;